const FIELDS = [
  'Фото (всі знімки з оголошення)',
  'Бренд, модель, тип обладнання',
  'Стан (Used/New тощо)',
  'Ціна, якщо вказана продавцем',
  'Рік випуску — з опису чи серійного номера, не дата публікації',
  'Гарантія, якщо згадана',
  'Опис оголошення повністю',
];

const PARTS_FIELDS = [
  'Парт-номер',
  'Короткий опис запчастини',
];

export default function Help() {
  return (
    <section className="help-section" id="help">
      <h2>Довідка</h2>
      <p>Встав один або кілька лінків на оголошення <strong>dotmed.com</strong> (кожен з нового рядка) — інструмент відкриє кожну сторінку і витягне:</p>
      <ul>
        {FIELDS.map((f) => <li key={f}>{f}</li>)}
      </ul>
      <p>Якщо оголошення — запчастина, додатково:</p>
      <ul>
        {PARTS_FIELDS.map((f) => <li key={f}>{f}</li>)}
      </ul>
      <p className="help-note">
        Можна вставити лінк на сторінку продавця (webstore/профіль) — інструмент сам знайде всі його оголошення. Обери вище, що саме тягнути: обладнання, запчастини, або і те, і те.
      </p>
    </section>
  );
}
